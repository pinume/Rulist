# Rulist 前端待办

记录尚未完成的前端优化工作。Logo 与视觉基础、目录浏览与搜索、Logo 返回首页、首页固定列表视图，以及目录页复制路径和子目录返回上级目录已完成；这些功能仍需完成下方所列的浏览器端验收。

## 1. 浏览器端验收

在可用浏览器环境中，对真实页面进行视觉与交互验收并记录结果：

- 浅色和深色模式；桌面与移动端布局。
- 长文件名，以及 Maple Mono NF CN 等宽字体下中英文文件名的可读性。
- 当前已加载页（每页 100 项）的过滤；`/` 聚焦搜索、`Esc` 清空并退出。
- 过滤后的选择、右键菜单和批量操作；点击 Logo 返回首页。
- 目录页顶部“复制路径”：分别在根目录和子目录复制并核对站内路径；验证剪贴板不可用或写入失败时有明确反馈。
- 子目录列表首行“返回上级目录”：确认仅在子目录出现，点击可返回父目录；过滤时行为正确，且该行不参与文件选择和批量操作。
- 文件上传、下载及相关回归检查。

此前环境没有可用浏览器，因此目前只有 HTTP、构建和资源交付等验收记录，浏览器视觉与交互尚未实测。搜索按既定设计只过滤当前已加载页，不包含全目录搜索。

## 2. 第三阶段：文件预览

逐步建立共用的 `FilePreviewLayout`：保留面包屑，展示文件名与元信息，提供下载和次要操作入口，并为各类 Viewer 提供预览区域。保持 SolidJS、等宽字体偏好和文件管理能力；逐类确认文件大小、失败提示、原始地址访问、认证、隐私及跨域限制。实现前先检查现有能力，不预设新增依赖或后端改动。

### P1：Markdown、PDF、图片、文本 / 代码

- **Markdown：** 保持 Maple Mono NF CN 全站等宽字体，设置适合阅读的正文宽度，完善标题、代码块、表格、图片、引用、链接和深色模式。
- **PDF、图片：** 在统一布局中预览内容，并提供下载；检查大文件和加载失败时的表现。
- **文本 / 代码：** 支持长内容与横向滚动；适用时提供行号、复制、Raw 和下载操作。

### P2：CSV、JSON、XML

- **CSV：** 表头固定，支持横向滚动，并为长单元格设置合理最大宽度；避免大量列被压得过窄。
- **JSON / XML：** 提供易读的结构化或代码式展示，处理无效内容和长文件，并保留 Raw 与下载入口。

### P3：音频、视频、压缩包

- **音频 / 视频：** 使用浏览器原生能力时验证格式支持、播放失败和下载路径。
- **ZIP / TAR 等压缩包：** 明确预览能力与限制；检查大文件及解析失败时的提示，不影响原文件下载。

各类预览按优先级独立交付和验收，避免一次性扩大范围。

## 已有验收记录

- [第一阶段视觉与 Logo](verification_phase1_visual.json)
- [第二阶段浏览与搜索](verification_phase2_browse.json)
- [首页列表视图](verification_list_only.json)
- [2026-09-25 部署验收](verification_deployment_20260925.json)
- [2026-09-25 Maple 字体部署验收](verification_maple_font_deployment_20260925.json)
- [2026-09-25 复制路径与返回上级目录验收](verification_copy_path_parent_20260925.json)

这些记录不代表浏览器视觉或交互已通过验收。
